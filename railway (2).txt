[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node index.js"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_error"
restartPolicyMaxRetries = 3

[build.environment]
NODE_ENV = "production"
PORT = "3000"

[variables]
PORT = "3000"
NODE_ENV = "production"