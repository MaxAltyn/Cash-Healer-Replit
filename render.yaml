services:
  - type: web
    name: cash-healer-bot
    env: node
    buildCommand: |
      npm install
      npm run build || true
    startCommand: ./render-start.sh
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /