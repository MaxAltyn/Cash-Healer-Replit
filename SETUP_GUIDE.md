# 🤖 Руководство по настройке и развертыванию Telegram бота

## ✅ Что уже готово

- ✅ Полностью настроенная база данных PostgreSQL
- ✅ Telegram бот с интеграцией YooKassa для приема платежей
- ✅ Workflow для обработки заказов и отправки документов
- ✅ Агент с памятью для персонализированных диалогов
- ✅ Инструменты для работы с Telegram API и базой данных

## 📋 Необходимые переменные окружения

Перед публикацией бота вам необходимо настроить следующие переменные:

### 1. Telegram Bot Token
Получите токен от [@BotFather](https://t.me/botfather):
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot` (если создаёте нового бота) или `/mybots` (если редактируете существующего)
3. Следуйте инструкциям и скопируйте токен
4. Добавьте переменную: `TELEGRAM_BOT_TOKEN=ваш_токен`

### 2. YooKassa Credentials
Получите ключи API от [YooKassa](https://yookassa.ru/):
1. Зарегистрируйтесь в [личном кабинете YooKassa](https://yookassa.ru/)
2. Перейдите в раздел "Интеграция" → "API"
3. Скопируйте Shop ID и Secret Key
4. Добавьте переменные:
   - `YOOKASSA_SHOP_ID=ваш_shop_id`
   - `YOOKASSA_SECRET_KEY=ваш_secret_key`

### 3. Настройка администраторов
Чтобы сделать пользователя администратором:
1. Узнайте Telegram ID пользователя (можно через бота [@userinfobot](https://t.me/userinfobot))
2. Добавьте в базу данных через "Database" вкладку в Replit:
   ```sql
   UPDATE users SET "isAdmin" = true WHERE "telegramId" = 'ваш_telegram_id';
   ```

## 🚀 Как опубликовать бота

### Вариант 1: Через кнопку "Publish" в Replit
1. Нажмите кнопку **"Publish"** в правом верхнем углу
2. Следуйте инструкциям для настройки домена
3. После публикации бот автоматически начнёт работать

### Вариант 2: Настройка webhook вручную
После публикации настройте webhook для Telegram:
```bash
curl -X POST "https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook" \
  -d "url=https://ваш_домен.replit.app/webhooks/telegram/action"
```

## 📱 Как использовать бота

### Для студентов (обычных пользователей):
1. Откройте бота в Telegram: `https://t.me/ваш_бот_username`
2. Отправьте `/start` для начала работы
3. Выберите услугу:
   - **Финансовый детокс** (450₽) - одноразовая услуга с персональным PDF отчётом
   - **Финансовое моделирование** (350₽) - доступ к автоматизированному алгоритму
4. Оплатите через YooKassa
5. Заполните форму (для Финансового детокса)
6. Получите документы от исполнителя

### Для администраторов:
1. Отправьте `/start` боту
2. Вы увидите дополнительные кнопки:
   - **Ожидающие заказы** - список оплаченных заказов
   - **Отправить документы** - загрузка PDF и Excel файлов пользователям
3. Для отправки документов:
   - Выберите заказ из списка
   - Загрузите PDF отчёт
   - Загрузите Excel таблицу
   - Документы автоматически отправятся пользователю

## 🔧 Тестирование на Playground

Перед публикацией вы можете протестировать бота:
1. Откройте вкладку **"Playground"** в Replit
2. Выберите агента **financialBotAgent**
3. Протестируйте различные сценарии:
   - Заказ услуги
   - Оплата (в тестовом режиме)
   - Просмотр заказов

## 📊 Цены услуг

Текущие цены настроены в коде:
- **Финансовый детокс**: 450₽ (можно изменить в `src/mastra/agents/financialBotAgent.ts`)
- **Финансовое моделирование**: 350₽ (можно изменить там же)

## ❓ Важные замечания

1. **После каждого изменения кода** необходимо заново опубликовать бота, чтобы изменения вступили в силу
2. **База данных** автоматически создаётся при первом запуске (миграции не требуются)
3. **Логи** доступны во вкладке "Logs" в Replit для отладки
4. **Yandex Forms** используется для сбора детальной информации (ссылка: https://forms.yandex.ru/u/6912423849af471482e765d3)

## 🆘 Поддержка

Если возникли вопросы:
- Проверьте логи во вкладке "Logs"
- Убедитесь, что все переменные окружения настроены правильно
- Проверьте, что webhook настроен корректно через API Telegram
